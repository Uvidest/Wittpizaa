<section class="custom-collection-list">
  <div class="page-width">
    <h1 class="custom-collection-list__title">{{ section.settings.title }}</h1>
    <div class="custom-collection-list__wrapper">
      {% for item in section.blocks %}
        <div class="custom-collection-list__item">
          {{ item.settings.collection.featured_image | image_url: width:592 | image_tag }}
          <a class="custom-collection-list__link" href="{{item.settings.collection.url}}">{{item.settings.collection.title}}</a>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
<style>
  .custom-collection-list{
    background-color: #000;
  }
  .custom-collection-list__title {
    color: #fff;
    text-align: center;
    font-family: Oswald;
    font-size: 64px;
    font-weight: 700;
    line-height: normal;
    text-transform: uppercase;
    margin: 0;
    padding: 48px 0;
  }
  .custom-collection-list__wrapper {
    display: flex;
    gap: 24px;
    padding-bottom: 48px;
  }
  .custom-collection-list__item {
    position: relative;
    flex-basis: calc(33% - 12px);
  }
  .custom-collection-list__item > img {
    width: 100%;
    height: auto;
    object-fit: cover;
    border-radius: 20px;
  }
  .custom-collection-list__link {
    position: absolute;
    text-decoration: none;
    left: 24px;
    bottom: 24px;
    color: #171719;
    font-family: Oswald;
    font-size: 24px;
    font-style: normal;
    font-weight: 600;
    line-height: 59px; 
    text-transform: uppercase;
    background-color: #fff;
    border-radius: 50px;
    padding: 0 50px;
  }
  @media (max-width: 992px) {
    .custom-collection-list__wrapper {
      flex-wrap: wrap;
      justify-content: center;
    }
    .custom-collection-list__item {
      flex-grow: 1;
      max-width: 440px;
      min-width: 345px;
    }
  }
  @media (max-width: 768px) {
    .custom-collection-list__title{
      font-size: 40px;
    }
  }
</style>
{% schema %}
{
    "name": "custom-collection-list",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title for section"
      }
    ],
    "blocks": [
      {
        "type": "collection-item",
        "name":"collection item",
        "settings": [
          {
            "type": "collection",
            "id": "collection",
            "label": "Collection"
          }
        ] 
      }
    ],
    "presets": [
      {
        "name": "custom-collection-list"
      }
    ]
  }
{% endschema %}